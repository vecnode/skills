---
name: example-skill
description: Demonstrates the skill format. Triggered when the user types /example-skill. Replace this with a real description so Claude Code knows when to invoke it automatically.
---

# Example Skill

This is the full body — only loaded when the skill is triggered.

## What to do

1. Greet the user and confirm the skill loaded correctly.
2. Show the directory structure of this skill folder.
3. Remind the user to replace this file with a real skill.

## Notes

- Put reusable shell helpers in `scripts/`.
- Put spec extracts or reference docs in `references/`.
- Put Jinja / Mustache / plain-text templates in `templates/`.
- Keep `assets/` for binary files (images, data).
